export default function AdminPage() {
  return (
    <main style={{ padding: 'var(--space-5)', color: 'var(--color-text-primary)' }}>
      <h1>Admin Panel</h1>
      <p style={{ color: 'var(--color-text-secondary)', marginTop: 'var(--space-2)' }}>
        Phase 7 — Admin panel coming soon
      </p>
    </main>
  )
}
